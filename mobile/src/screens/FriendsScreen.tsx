import React from 'react';
import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export function FriendsScreen() {
  return (
    <ScreenPlaceholder
      title="Friends"
      note="Friend activity, joined against the follow graph and visible_logs view. Needs Phase 3 (Feed + Social)."
    />
  );
}
