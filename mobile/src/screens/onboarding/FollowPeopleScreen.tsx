import React from 'react';
import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export function FollowPeopleScreen() {
  return (
    <ScreenPlaceholder
      title="Follow some curators"
      note="Onboarding step 4 of 4: suggested tastemakers/curators to follow before the Feed has anything in it. Completing this screen should navigate into Main, not push another step."
    />
  );
}
